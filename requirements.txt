# Analysis environment for interface-bulge-dent-de
# Python 3.11+
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-image>=0.21      # marching_cubes for the open interface sheet
trimesh>=4.0            # sheet smoothing / normals
anndata>=0.10
scanpy>=1.10
matplotlib>=3.7
# The open interface sheet is extracted directly from the labelled point cloud
# by src/open_interface.py — no external mesh-building package is required.
