# Analysis environment for interface-bulge-dent-de
# Python 3.11+
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
trimesh>=4.0
anndata>=0.10
scanpy>=1.10
matplotlib>=3.7
# Interface extraction (upstream 3DCurv) provides st3d_interface.compute_interface;
# it is not on PyPI — see DATA.md / the 3DCurv project.
# GO enrichment (optional, network): gprofiler-official
