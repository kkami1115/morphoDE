# raw data — never commit (see DATA.md for sources)
*.h5ad
*.h5ad.zst
*.loom
raw/
# ignore downloaded dataset payloads, but keep the ledger + downloader lockfiles
data/*
!data/catalog.csv
!data/**/CHECKSUMS.sha256
# python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
# os
.DS_Store
